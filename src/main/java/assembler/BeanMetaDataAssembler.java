package assembler;

import entity.BeanMetaData;

public class BeanMetaDataAssembler {

    public static BeanMetaData assemble() {
        BeanMetaData beanMetaData = new BeanMetaData();

        return beanMetaData;
    }
}
